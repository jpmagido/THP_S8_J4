<div>
	<div>
		<b><u> Objet:</u></b> <%= @email_show_objet %>
	</div>
	<div>
		<u> Contenu: </u> <%= @email_show_body %>
	</div>
<br>
<%= link_to "Supprimer", email_path, method: :delete, class: "btn btn-danger", :data => {:confirm => 'Are you sure?'}, remote: true%>

<%if @email_one.read == true %>
	<%= link_to "Mail Non lu", email_path, method: :patch, class: "btn btn-warning", remote: true%>
<% else %>
<%= link_to "Mail Lu", email_path, method: :patch, class: "btn btn-warning", remote: true%>
<% end  %>

</div>
<br><hr>
